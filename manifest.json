{
 "manifest_version":2,

 "name": "Bulkhead",
 "description": "Append a string to the current tab's URL, after the TLD section. Useful for UWaterloo library proxy use.",
 "version": "0.2",

 "browser_action": {
     "default_icon": "bulkhead.png"
 },

 "background": {
    "scripts": ["background.js"],
    "persistent": false
  },

 "permissions": [
     "tabs",
     "activeTab"
 ]

}
